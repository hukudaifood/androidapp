# 福大メシ・ルーレット — デザイン仕様書

## アプリ概要

| 項目 | 内容 |
|------|------|
| アプリ名 | フクメシ |
| ターゲット | 福岡大学の学生・教職員 |
| 目的 | 「今日のランチ何食べよう」を解決するルーレットアプリ |
| テーマカラー | オレンジ `#FF8000` |
| 対応OS | Android 7.0 (API 24) 以上 |

---

## カラーパレット

| 役割 | ライトモード | ダークモード |
|------|-------------|-------------|
| Primary | `#FF8000` (オレンジ) | `#FFAB5E` (薄オレンジ) |
| Primary Container | `#FFE0C0` | `#6B2E00` |
| Secondary | `#775A40` | `#E6BF9A` |
| Secondary Container | `#FFDDBB` | `#5C4128` |
| Background | `#FFFBFE` | `#1C1B1F` |
| Surface Variant | `#F3E0CC` | `#52443A` |
| Error | `#B3261E` | `#F2B8B5` |

---

## 画面一覧と遷移

```
[ルーレット画面] ←→ [一覧画面]   ← BottomNavigation で切り替え
       ↓                 ↓
  [詳細画面]        [詳細画面]
```

---

## 1. ルーレット画面 (RouletteScreen)

**役割:** アプリのメイン画面。ボタンを押すとお店がスロットマシン風に切り替わり、1件をランダム決定する。

### レイアウト構成

```
┌─────────────────────────────┐
│  TopAppBar                  │
│  フクメシ   [≡]  │
├─────────────────────────────┤
│                             │
│  ┌─────────────────────┐   │
│  │  スロットマシン表示   │   │
│  │  (高さ 260dp)        │   │
│  │                     │   │
│  │  [画像 120×120]     │   │
│  │  店舗名             │   │
│  │  ジャンル名         │   │
│  │  "タップして詳細 →" │   │
│  └─────────────────────┘   │
│                             │
│  ┌─────────────────────┐   │
│  │  結果カード（決定後）  │   │
│  │  今日のおすすめ      │   │
│  │  [店舗名]           │   │
│  │  ジャンル | 価格帯   │   │
│  │  [詳細を見る]       │   │
│  └─────────────────────┘   │
│                             │
│  ┌─────────────────────┐   │
│  │  ルーレットを回す    │   │  ← Primary色の大きいボタン
│  └─────────────────────┘   │
│                             │
│  [ラーメン] [〜500円] ...   │  ← 適用中フィルターのチップ
│                             │
├─────────────────────────────┤
│  BottomNavigation           │
│   🎰ルーレット  📋一覧      │
└─────────────────────────────┘
```

### 状態別UI

| 状態 | 表示内容 |
|------|---------|
| 初期状態 | 「ルーレットを回してください」というプレースホルダーテキスト |
| スピン中 | 1.5秒間50ms間隔でお店カードがスライドアニメーションで切り替わる。ボタンは「選択中...」に変わりdisabled |
| API待機中 | スロット切り替えが続き、API結果を待機 |
| 決定後 | スロットが止まり選ばれたお店を表示。結果カードも下に出る |
| エラー | Snackbarでエラーメッセージを表示 |

### スロットマシン表示カードの要素

- **サムネイル画像:** 120×120dp、角丸(medium)、Coilで非同期ロード
- **店舗名:** `titleLarge`、最大2行
- **ジャンル名:** `bodyMedium`、`onSurfaceVariant`色
- **タップ誘導テキスト:** 「タップして詳細を見る →」（決定後のみ表示）
- **カードのタップ:** 詳細画面へ遷移（スピン中はdisabled）

### フィルターボトムシート（フィルターアイコンタップで表示）

```
┌─────────────────────────────┐
│  ▬▬▬                       │  ← ドラッグハンドル
│                             │
│  ジャンル                   │
│  [すべて] [定食] [中華]     │
│  [ラーメン] [カレー] [うどん]│
│  [牛丼] [とんかつ] [焼肉]   │
│  [居酒屋] [カフェ] [その他] │
│                             │
│  価格帯                     │
│  [すべて] [〜500円]         │
│  [500〜1000円] [1000円〜]   │
│                             │
│  営業状況                   │
│  [営業中のみ]               │
│                             │
└─────────────────────────────┘
```

---

## 2. 一覧画面 (RestaurantListScreen)

**役割:** 条件でフィルタリングしたお店の一覧を表示する。

### レイアウト構成

```
┌─────────────────────────────┐
│  TopAppBar                  │
│  お店一覧              [≡]  │
├─────────────────────────────┤
│  カテゴリ横スクロール        │
│  [すべて][営業中][定食][中華]│  ← 横スクロール FilterChip
├─────────────────────────────┤
│                             │
│  ┌─────────────────────┐   │
│  │ RestaurantCard      │   │
│  │ [画像] 店舗名        │   │
│  │        ジャンル|価格 │   │
│  │        住所         │   │
│  └─────────────────────┘   │
│  ┌─────────────────────┐   │
│  │ RestaurantCard      │   │
│  └─────────────────────┘   │
│       ...                   │
│                             │
├─────────────────────────────┤
│  BottomNavigation           │
└─────────────────────────────┘
```

### 状態別UI

| 状態 | 表示内容 |
|------|---------|
| ローディング（初回） | 中央に CircularProgressIndicator |
| データあり | RestaurantCardのリスト |
| エラー（データなし） | エラーメッセージ + 「再試行」ボタン |
| エラー（データあり） | Snackbarでエラーメッセージ（既存リストはそのまま表示） |

### RestaurantCard の要素

```
┌─────────────────────────────────┐
│  [画像]  店舗名（1行、省略）     │
│  80×80   ジャンル | 価格帯      │
│  角丸    住所（1行、省略）       │
└─────────────────────────────────┘
```

- カード全体がタップ可能 → 詳細画面へ
- 画像: 80×80dp、角丸(small)、Coilで非同期ロード
- elevation: 2dp

### カテゴリ横スクロールの要素

- 「すべて」「営業中」に加え、全ジャンル(11種)がChipとして並ぶ
- 選択中のChipは強調表示(selected=true)
- 複数ジャンルの同時選択が可能

---

## 3. 詳細画面 (RestaurantDetailScreen)

**役割:** 選んだお店の詳細情報を表示する。

### レイアウト構成

```
┌─────────────────────────────┐
│  TopAppBar                  │
│  ← [店舗名]                 │
├─────────────────────────────┤
│  [ヘッダー画像 全幅×200dp]  │  ← imageUrlがある場合のみ表示
├─────────────────────────────┤
│                             │
│  店舗名 (headlineMedium)    │
│                             │
│  ジャンル名  |  価格帯       │  ← Primaryカラー | 標準カラー
│                             │
│  📍 住所テキスト            │
│                             │
│  🕐 営業時間 〜 閉店時間    │  ← 時間情報がある場合のみ
│                             │
│  ┌─────────────────────┐   │
│  │  🗺  地図で見る       │   │  ← 全幅ボタン（座標orURL有の場合）
│  └─────────────────────┘   │
│                             │
└─────────────────────────────┘
```

### 状態別UI

| 状態 | 表示内容 |
|------|---------|
| ローディング | 中央に CircularProgressIndicator |
| データあり | 詳細コンテンツ（縦スクロール対応） |
| エラー | エラーメッセージ + 「再試行」ボタン |

### 地図ボタンの動作

1. `googleMapsUrl` が設定されていればそのURLを開く
2. なければ `geo:緯度,経度?q=緯度,経度(店舗名)` のURIを生成
3. 地図アプリが未インストールの場合は何もしない（クラッシュしない）

---

## 4. 共通コンポーネント

### BottomNavigation

| タブ | アイコン | ラベル |
|------|---------|-------|
| ルーレット | 🎰 Casino | ルーレット |
| 一覧 | 📋 List | 一覧 |

- 詳細画面では BottomNavigation を非表示
- タブ切り替えで状態を保持（`saveState = true`）

### FilterBottomSheet（共通）

ルーレット画面・一覧画面の両方で同じコンポーネントを使用。

| フィルター | 選択肢 |
|-----------|--------|
| ジャンル | すべて / 定食 / 中華 / ラーメン / カレー / うどん / 牛丼 / とんかつ / 焼肉 / 居酒屋 / カフェ / その他 |
| 価格帯 | すべて / 〜500円 / 500〜1000円 / 1000円〜 |
| 営業状況 | 営業中のみ |

### LoadingContent

画面中央に `CircularProgressIndicator` を表示。

### ErrorContent

```
       エラーが発生しました
   [エラーメッセージテキスト]

          [再試行]
```

---

## 5. ドメインモデル

### Restaurant（お店）

| フィールド | 型 | 説明 |
|-----------|-----|------|
| id | String | 店舗ID |
| name | String | 店舗名 |
| genre | Genre | ジャンル |
| priceRange | PriceRange | 価格帯 |
| address | String | 住所 |
| latitude | Double? | 緯度 |
| longitude | Double? | 経度 |
| imageUrl | String? | 画像URL |
| googleMapsUrl | String? | Google MapsのURL |
| openingHours | String? | 開店時間 |
| closingHours | String? | 閉店時間 |

### Genre（ジャンル enum）

`すべて` / `定食` / `中華` / `ラーメン` / `カレー` / `うどん` / `牛丼` / `とんかつ` / `焼肉` / `居酒屋` / `カフェ` / `その他`

### PriceRange（価格帯 enum）

`すべて(0)` / `〜500円(1)` / `500〜1000円(2)` / `1000円〜(3)`

---

## 6. APIエンドポイント

Base URL: `https://fukudaifood-130668695114.asia-northeast1.run.app/`

| メソッド | パス | 説明 |
|---------|------|------|
| GET | `v1/restaurants` | 一覧取得（genre/price_range/is_open_nowでフィルター） |
| GET | `v1/restaurants/{id}` | 詳細取得 |
| POST | `v1/roulette` | ルーレット実行（ランダム1件を返す） |

---

## 7. 画面遷移フロー

```
起動
 └→ ルーレット画面（スタート）
      ├─ [ルーレットを回す] → APIコール → 結果表示
      ├─ [スロットカードタップ] → 詳細画面
      ├─ [フィルターアイコン] → FilterBottomSheet（モーダル）
      └─ [BottomNav: 一覧] → 一覧画面
           ├─ [カードタップ] → 詳細画面
           │    └─ [地図で見る] → 外部地図アプリ（別アプリ起動）
           └─ [フィルターアイコン] → FilterBottomSheet（モーダル）
```

---

## 8. デザイン上の注意点

- **ダイナミックカラー:** Android 12以上では端末のダイナミックカラーが優先される（`dynamicColor = true`）。そのためオレンジのブランドカラーは古いAndroidや一部端末でのみ適用される
- **エッジtoエッジ:** `enableEdgeToEdge()` が有効。ステータスバー・ナビゲーションバー領域を考慮したレイアウトが必要
- **ダークモード対応済み:** ライト/ダーク両方のカラーシームが定義されている
- **画像のフォールバック:** `imageUrl` がnullまたはロード失敗時のプレースホルダーは現状未設定（Coilのデフォルト表示）
